    <div class="header">
    <%= render "/shared/header" , user: @random.user%>
    </div>

    <div id="main-random">

    <div id="random-title">今日の献立</div>

    <%= form_with model: @random, url: randoms_search_path, method: :GET, id: "form", local: true do |f| %>

    <%= f.collection_select :type_of_food_id, TypeOfFood.all, :id, :name,{} %>

      <%= f.submit "ランダム", id: "submit-btn"%>
    <%end %>

    </div>